// Path to the bundled preload script produced by `npm run bundle`
const PRELOAD_PATH_DIST = 'dist/preload.js';
// Directory containing renderer bundle and version info
const BUILD_DIR = 'build/unpacked';
module.exports = { PRELOAD_PATH_DIST, BUILD_DIR };
